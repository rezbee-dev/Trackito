# Trackito

## Commands

Run docker compose
- `DISCORD_TOKEN="your_secret_token" docker compose up -d --build`

Stop docker compose containers (perserves data)
- `docker compose stop`

Remove docker compose containers (removes everything)
- `docker compose down`